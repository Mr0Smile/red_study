<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.StudybaseformMapper">

    <select id="selectAll" resultType="com.example.entity.StudyBaseform">
        select study_baseform.* from study_baseform
        <where>
            <if test="studyBaseformName != null"> and study_baseform.study_baseform_name like concat('%', #{studyBaseformName}, '%')</if>
        </where>
        order by study_baseform.id desc
    </select>

    <insert id="insert" parameterType="com.example.entity.StudyBaseform" useGeneratedKeys="true">
        insert into study_baseform(id, study_baseform_name,descr,article_id,special_column_id)
        values (#{id}, #{studyBaseformName},#{descr},#{articleId},#{specialColumnId})
    </insert>

    <update id="updateById" parameterType="com.example.entity.StudyBaseform">
        update study_baseform set study_baseform_name = #{studyBaseformName},descr=#{descr},article_id=#{articleId},special_column_id=#{specialColumnId}
        where id = #{id}
    </update>

</mapper>